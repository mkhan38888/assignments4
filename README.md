# assignments4

To run this code, Python 3 must be installed on your system. Open a terminal in the project directory and run the program using the command python filename.py (or python3 filename.py on some systems).
